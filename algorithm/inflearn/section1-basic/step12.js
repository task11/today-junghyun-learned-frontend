function solution(str) {
  let answer = str.toUpperCase();

  // for (let x of str) {
  //   if (x === x.toUpperCase()) {
  //     answer++
  //   }
  // }

  return answer;
}

console.log(solution("ItisTimeToStudy"));